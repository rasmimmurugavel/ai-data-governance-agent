# Core agent + UI
# >=1.50 for st.plotly_chart/st.dataframe's `width="stretch"` param
# (use_container_width is deprecated as of this pin).
streamlit>=1.50.0
anthropic>=0.34.0
# Pinned to the 1.x line deliberately: mcp 2.0 (released very recently)
# renamed FastMCP -> MCPServer and changed server-side internals. The
# client-side API this app also uses (ClientSession, StdioServerParameters,
# stdio_client) is unchanged across 1.x/2.x, but mcp_server/server.py
# uses `mcp.server.fastmcp.FastMCP`, which only exists on 1.x. Revisit
# this pin deliberately (see mcp_server/server.py) if migrating to 2.x.
mcp>=1.2.0,<2.0.0

# Postgres
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.0
sqlglot>=25.0.0

# Data profiling / quality
pandas>=2.2.0
numpy>=1.26.0

# Config, validation, PII detection
pydantic>=2.8.0
python-dotenv>=1.0.0
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355

# Charts
plotly>=5.24.0

# Eval / rubrics
pyyaml>=6.0.2
jsonschema>=4.23.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
